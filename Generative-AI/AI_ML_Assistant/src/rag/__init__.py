"""RAG (Retrieval-Augmented Generation) components.

This package handles submodules::
- ``embeddings`` — converting text into vectors using local models or APIs.
- ``retriever`` — finding relevant documents using Chroma, BM25, and hybrid search.
- ``ingest`` — loading documents, splitting text into chunks, and building the index.
"""
